flask
flask-cors
gpiozero
pigpio        # GPIO backend for RPi 3/4 (lgpio installed dynamically for RPi 5)
apscheduler
ntplib        # NTP time sync — keeps schedules accurate without an RTC chip
pytz          # Timezone support

# Firebase Admin SDK — writes attendance to Firestore from the Pi
firebase-admin

# Face-detection stack (optional - uncomment to enable attendance scanning)
# deepface
# tensorflow==2.15.0
# tf-keras
# opencv-python-headless
# Note: picamera2 is pre-installed on Raspberry Pi OS Bullseye+
# Note: lgpio is installed automatically by the startup script on Raspberry Pi 5
