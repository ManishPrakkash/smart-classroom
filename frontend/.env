# ── Smart Switch Frontend – Environment Variables ────────────────────────────
# Vite only exposes variables prefixed with VITE_ to the browser.

# IP address of the Raspberry Pi running the Flask backend.
# Use "localhost" for local dev (backend running on same machine).
# Use the Pi's IP (e.g. 10.27.253.96) when testing from mobile/other devices.
# KK 10.27.253.96
VITE_PI_IP=10.27.253.96

# Backend port (must match Flask app.run port)
VITE_API_PORT=8000
